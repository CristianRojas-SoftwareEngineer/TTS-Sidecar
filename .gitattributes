# Todos los archivos de texto se normalizan a LF en el repo y en el working tree
# (detección automática de texto/binario; los de texto siempre con final de línea LF).
* text=auto eol=lf

# Archivos binarios - no modificar
*.bin     binary
*.png     binary
*.jpg     binary
*.wav     binary
*.mp3     binary
