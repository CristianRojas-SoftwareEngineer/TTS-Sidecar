node_modules/
build/
dist/
dist_test/
assets/
# Voces de usuario ignoradas, pero la voz de fábrica 'default' se versiona
# (se empaqueta vía --add-data y sirve de voz por defecto).
voices/*
!voices/default/
# Artefactos de build del AppImage
AppDir/
tts-sidecar.png
*.spec
# Config de Claude Code: versionar las skills; ignorar archivos de sesión
# (p. ej. continuity-prompt.md, settings.local.json).
.claude/*
!.claude/skills/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
